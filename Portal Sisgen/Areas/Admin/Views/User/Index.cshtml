@model IEnumerable<ApplicationUser>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br /><br />
<div class="container">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info"> Lista de usuarios</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-info"><i class="fas fa-plus"></i> &nbsp; Usuario Nuevo</a>
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border ">
                <tr class="table-secondary">
                    <th>
                        Nombre
                    </th>
                    <th>
                        Email
                    </th>
                    <th>
                        Telefono
                    </th>
                    <th>
                       Razon social

                    </th>
                    
                    <th>
                        Accion
                    </th>
                    
                </tr>
                @foreach (var item in Model)
                {
            <tr>
                <td>
                    @Html.DisplayFor(m => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(m => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(m => item.PhoneNumber)
                </td>
                <td>
                   @Html.DisplayFor(m=> item.RazonSocial)
                </td>
                @* Esta parte es de una vista parcial y ya tiene su propio td *@
                    <partial name="_TableButtonPartial" model="item.Id">
                
               

                
                @*BLOQUEO*@
                @*<td>
            @if (item.LockoutEnd == null || item.LockoutEnd < DateTime.Now)
            {
                <a class="btn btn-success text-white" asp-action="Lock" asp-route-id="@item.Id">
                    <i class="fas fa-lock-open"></i>
                </a>
            }
            else
            {
                <a class="btn btn-danger text-white" asp-action="UnLock" asp-route-id="@item.Id">
                    <i class="fas fa-lock"></i>
                </a>
            }
        </td>*@
            </tr>
                }
            </table>
        }
        else
        {
            <p>No existen Registros...</p>
        }
    </div>
</div>

